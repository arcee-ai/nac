[package]
name = "nac-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "nac_core"

[features]
default = []
test-support = []

[dependencies]
tokio = { version = "1", default-features = false, features = ["fs", "io-util", "macros", "net", "process", "rt-multi-thread", "sync", "time"] }
reqwest = { version = "0.13", default-features = false, features = ["json", "form", "rustls", "stream"] }
futures-util = { version = "0.3", default-features = false, features = ["std"] }
bytes = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
rusqlite = { version = "0.32", features = ["bundled"] }
uuid = { version = "1", features = ["serde", "v4"] }
libc = "0.2"
portable-pty = "0.8"
rmcp = { version = "1.3.0", default-features = false, features = ["client", "reqwest", "transport-child-process", "transport-streamable-http-client-reqwest"] }
toml = "0.8"
toml_edit = "0.22"
url = "2"
serde_yaml_ng = "0.10"
diffy = { version = "0.5.0", default-features = false }
fs2 = "0.4"
sha2 = "0.10"
rand = "0.9"
