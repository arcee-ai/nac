/**
 * Where the build put MathJax's CHTML fonts. Defined in `vite.config.ts`, which
 * is also what copies them there, so the URL cannot drift from the files.
 */
declare const __MATHJAX_FONT_URL__: string;
